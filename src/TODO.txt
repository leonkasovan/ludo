1. Create config.lua and implement ludo.setting.maxThread, ludo.setting.maxDownloadRetry
2. Implement ludo.http_tester.response.header(). ludo.http_tester.response.content() ludo.http_tester.request()
3. Find downloads history
4. Plugins LUA: convert output audio , convert images into cbs.