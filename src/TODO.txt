1. Implement robustly these lua script functions:
    ludo.http_tester.response.content(): return http response content as string from output_entry in on_http_clicked(gui.c)
    ludo.http_tester.response.header(): return http response header as string from header_resp_entry in on_http_clicked(gui.c)
    ludo.lua_tester.println(string message): print message to output_entry in function on_lua_clicked (gui.c)
    ludo.lua_tester.printf(string format, ...): print formatted message to output_entry in function on_lua_clicked (gui.c)

2. Add listbox for snippet selection in on_lua_clicked. The snippets will be stored in a folder named "snippets" and the listbox will display the names of the snippets. When a snippet is double-clicked, its content will be loaded into the Lua script tester for execution.
3. Create config.lua and implement ludo.setting.maxThread, ludo.setting.maxDownloadRetry
4. Find downloads history
5. Plugins LUA: convert output audio , convert images into cbs.